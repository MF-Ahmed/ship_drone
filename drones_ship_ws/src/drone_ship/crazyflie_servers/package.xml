<?xml version="1.0"?>
<package format="3">
  <name>crazyflie_servers</name>
  <version>0.0.1</version>
  <description>Multi-drone exploration and tracking action servers for Crazyflie simulation.</description>

  <maintainer email="you@example.com">Arham</maintainer>
  <license>MIT</license>

  <!-- Build tools -->
  <buildtool_depend>ament_cmake</buildtool_depend>
  <buildtool_depend>rosidl_default_generators</buildtool_depend>

  <!-- Interface/runtime -->
  <exec_depend>rosidl_default_runtime</exec_depend>

  <!-- Deps used in code and/or actions -->
  <depend>rclcpp</depend>
  <depend>rclcpp_action</depend>
  <depend>std_msgs</depend>
  <depend>geometry_msgs</depend>
  <depend>sensor_msgs</depend>
  <depend>nav_msgs</depend>
  <depend>tf2</depend>
  <depend>tf2_ros</depend>
  <depend>tf2_geometry_msgs</depend>
  <depend>action_msgs</depend>
  <depend>std_srvs</depend>
  <depend>builtin_interfaces</depend>
  <depend>crazyflie_yolo</depend>
  <depend>visualization_msgs</depend>

  <!-- Add this copy at top-level -->
  <member_of_group>rosidl_interface_packages</member_of_group>

  <export>
    <build_type>ament_cmake</build_type>
    <!-- Keep this copy in <export> as well -->
    <member_of_group>rosidl_interface_packages</member_of_group>
  </export>
</package>
